{
  "name": "music-scrape",
  "version": "0.0.2",
  "description": "Music Site Scrapers",
  "main": "src/index.js",
  "scripts": {
    "dev": "backpack && node build/main.js",
    "test": "mocha --require babel-core/register --require babel-polyfill --no-timeouts --exit"
  },
  "babel": {
    "presets": [
      "env"
    ]
  },
  "dependencies": {
    "babel-polyfill": "^6.26.0",
    "babel-runtime": "^6.26.0",
    "cheerio": "^1.0.0-rc.3",
    "express": "^4.17.1",
    "mongoose": "^5.7.3",
    "puppeteer": "^1.1.1",
    "request": "^2.88.0"
  },
  "devDependencies": {
    "babel-preset-env": "^1.6.1",
    "backpack-core": "^0.7.0",
    "chai": "^4.1.2",
    "mocha": "^5.0.4"
  }
}
